import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    minWidth: 275,
    marginTop: 15,
    width: "50vw",
    margin: "0 auto"
  },
  title: {
    fontSize: 14,
  },
  clock: {
    alignSelf: "center"
  }
});